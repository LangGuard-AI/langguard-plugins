{
  "mcpServers": {
    "langguard": {
      "type": "http",
      "url": "${user_config.api_endpoint}/api/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.api_key}"
      }
    }
  }
}
